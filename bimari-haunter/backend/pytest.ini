[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: tests that hit real external websites (deselect with -m "not live")
    facebook: tests that hit the Facebook Graph API (require FACEBOOK_ACCESS_TOKEN)
